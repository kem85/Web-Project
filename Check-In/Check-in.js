// Check-In logic is now handled by the backend (app.py) and Jinja templating.
// Form submissions are standard POST requests.
document.addEventListener("DOMContentLoaded", () => {
  // Any frontend-only UI logic can go here if needed in the future.
});
